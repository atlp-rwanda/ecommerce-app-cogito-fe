export interface Item {
    id: string;
    name: string;
    description: string;
    price: number;
    // Add other properties as needed
  }
  
  export interface CollectionItem {
    id: string;
    name: string;
    // Add other properties as needed
  }
  